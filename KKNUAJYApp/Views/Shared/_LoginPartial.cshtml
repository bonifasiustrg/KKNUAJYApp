@using KKNUAJYApp.Areas.Identity.Data
@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager
@* @inject UserManager<IdentityUser> UserManager *@

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
        <a  class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity?.Name!</a>
    </li>
    <li class="nav-item">
        <form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button  type="submit" class="nav-link btn btn-link text-dark">Logout</button>
        </form>
    </li>
}
else
{
        @* <li class="nav-item me-4">
            <button class="btn btn-outline-primary">
            <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Register</a>
        </button>
    </li> *@
        <li class="nav-item">
            <button class="btn btn-primary btn-sm px-3 py-2" style="background-color: #0A5896; border-radius: 6px ">
                <a class="text-light" style="font-size: 14px;font-weight: 600; text-decoration: none;" asp-area="Identity" asp-page="/Account/Login"> Login </a>
        </button>
    </li>
}
</ul>
